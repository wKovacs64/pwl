export interface LabelMap {
  number: string;
  uppercase: string;
  lowercase: string;
  special: string;
}

export default {
  number: 'Number',
  uppercase: 'Uppercase Letter',
  lowercase: 'Lowercase Letter',
  special: 'Special',
} as LabelMap;
